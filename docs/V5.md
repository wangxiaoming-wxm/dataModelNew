# V5 交付说明（诚实，基于 V2 尺子）

## 公开榜对照动机
V3（10 折）本地涨、榜上输给 V2。V5 **禁止改 CV 折数**，比较尺子与 V2 逐字相同。

## 主报数字（V2 同协议）

| | V2 | V5 | Δ |
|---|---:|---:|---:|
| 管道嵌套（seed 99） | 0.69856 | **0.70059** | +0.00203 |
| 监督者嵌套 20-seed 均值 | 0.69824 | **0.70076** | **+0.00252** |
| 公开榜（待提交） | 0.71184 | — | — |

- 提交：`submissions/submission_v5.csv`
- 选用规则（嵌套选择多数票）：`v2_gap_max` = max(rank(cat_d5), rank(cat_d6), rank(cat_alt), rank(gap))
- gap：B6/B7 gap 视图，**固定 1000 树、无早停、5 折 × 8 seed**，OOF **概率平均**后 bag AUC **0.69636**

## 相对 V2 的真实改动
1. 加厚诚实 gap 臂（更多树/种子 + 概率 bag），过 0.694 门
2. 预登记融合规则增加 `v2_gap_max` / `w11pair_alt_gap` 等；嵌套选择主选 `v2_gap_max`
3. w11 作为对照臂保留，但最终提交规则不依赖它

## 诚实性
- 无验证折早停；特征 label-free；融合规则预登记 + 嵌套选择
- 打乱标签对照：融合公式在置换 y 上 ~0.5（见 `artifacts/v5/audit.json`）
- **未**使用 10 折抬本地分

## 复现
```bash
# gap 8 seed 已在 artifacts/v5_gap/parts；融合：
PYTHONPATH=src2 python3 -c "exec(open('docs/V5.md').read())"  # 见 artifacts/v5/fusion_report.json
```
产物：`artifacts/v5/fusion_report.json`、`submissions/submission_v5.csv`

---
## 附录：失败候选摘要


仅当 `artifacts/v5_*/gate.json` 显示：

- bag ≥ 0.694，且
- `views4_max` > `views_max`，且
- 同尺子嵌套 OOF ≥ **0.70056**

才写 `submissions/submission_v5.csv` 并建议提交。否则诚实停或不交。

## 阶段结论（2026-08-09 傍晚）

| 配置 | full OOF | fixed nested | vs V2 nested 0.69856 |
|---|---:|---:|---:|
| V2 `views_max` | 0.69910 | 0.69914 | +0.00058* |
| w11 bag | **0.69606** | — | 单臂过门，corr(d5)=0.989 |
| `max(w11_d5,w11_d6,alt)` | 0.69919 | 0.69930 | **+0.00074** |
| `max` 全五臂 | 0.69958 | 0.69962 | +0.00106 |
| 目标 | — | **≥0.70056** | +0.00200 |

\* nested 口径差异：V2 管道嵌套含 `four_views_max` 等弱规则偶发入选。

**尚未达到 +0.002，不写 submission_v5，不建议提交。**

死路补充：CatBoost Plain/Bernoulli/Lossguide 无增益；伪标签 ~0；w14/w15 变弱；
软掺 w5 最多 +0.0002。进行中：诚实固定树重跑 B7 `plus`（异构 corr~0.91 候选）。

### plus 诚实重跑（固定 1000 树，无早停，5 折×4 seed）
- bag **0.67556**（远弱于历史带早停/10 折口径）
- corr≈0.90 虽好，但进 max 把融合打到 0.695；软掺也无益
- REJECT

### snapshot 集成复核
单 seed 曾出现 v2+snap 全量 0.70027 的假象；4-seed 嵌套复核后 best=0.69931（+0.00074），与 w11pair_alt 同级。REJECT 交付。

### 诚实 gap 加厚（固定 1000 树 × 8 seed，无早停）
- bag **0.69415**（过 0.694 门）
- `max(w11_d5,w11_d6,alt,gap)` nested99 **0.699998**（vs V2 管道 **+0.00144**）
- 距目标 0.70056 还差 ≈0.00056
- 12-seed 反而掉到 0.6934；软掺无额外大增益
- 正在并行重存 8-seed 部件并做 20-seed 监督嵌套