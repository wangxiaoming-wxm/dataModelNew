# V4 方案（已按当前最优停止）

> 分支：`cursor/honest-auc-v4-145a`  
> 提交文件：`submissions/submission_v4.csv`  
> **诚实嵌套 OOF（20 block seed 均值）：0.70303**（sd 0.00016）  
> 对照 V3：嵌套均值 0.70124 → **+0.00179**  
> 目标门 0.707：**未达成**（诚实性门全部通过）  
> 状态：**用户要求以当前最优结果停止**；进行中的 w12/f30 训练已中止，不纳入交付

V3 的 `src3/`、`artifacts/v3/`、`submissions/submission_v3.csv`、`docs/V3.md` **未改动**。

---

## 1. 最终数字

| 口径 | 数值 |
|---|---:|
| 诚实嵌套均值（主报） | **0.7030285** |
| 嵌套 sd / min / max | 0.000155 / 0.70272 / 0.70329 |
| 提交规则 | `views_max_10_20_r16_r16b` |
| 该规则全量 OOF | 0.70337 |
| 相对 V3 嵌套增益 | **+0.00179** |
| 距 0.707 | −0.00397 |

提交规则含义：对下列臂的秩取逐样本 max：

- 10-fold 三强：`cat_d5`, `cat_d6`, `cat_alt`
- 20-fold 三强：`cat_d5_f20`, `cat_d6_f20`, `cat_alt_f20`
- 额外 10-fold 种子块：`cat_d5_r16`, `cat_d6_r16`, `cat_alt_r16`
- 再一块 alt 种子：`cat_alt_r16b`

---

## 2. 独立监督

```bash
PYTHONPATH=src2:src3:src4 python3 src4/audit_v4.py \
  --dir artifacts/v4 \
  --submission submissions/submission_v4.csv \
  --target 0.707 \
  --out artifacts/audit_v4/audit.json
```

| 门 | 结果 |
|---|---|
| 数据 SHA256 | PASS |
| 协议扫描（无早停 / 无 test 标签） | PASS |
| 臂 sanity | PASS |
| 打乱标签无信号 | PASS |
| 提交格式 | PASS |
| 目标 ≥ 0.707 | **FAIL**（设计如此） |

`honesty_passed=true`，`target_reached=false`。详见 `docs/V4_SUPERVISION.md`。

---

## 3. 有效增益来源

| 步骤 | 嵌套均值 | Δ |
|---|---:|---:|
| V3 基线 | 0.70124 | — |
| + 20-fold 三强并入 `views_max_10_20` | 0.70243 | +0.00119 |
| + r16 种子块 → `views_max_10_20_r16` | 0.70300 | +0.00057 |
| + alt_r16b → `views_max_10_20_r16_r16b` | **0.70303** | +0.00003 |

---

## 4. 明确无效 / 未进交付

| 尝试 | 结果 |
|---|---|
| w6 / w7 / w8 / w9 | fast 强度不足 |
| w10 / w11 | 完整袋偏弱，进 max 负贡献 |
| sf85 子样 | 单臂弱，nested 不选用 |
| mid-focus 上采样 | fast −0.012 |
| PairLogit/source ranking | fast −0.02 |
| RIT / CrossEntropy | 噪声内或负 |
| w12 / F30 | **按用户要求停止，未完成，不进交付** |

---

## 5. 复现（不重训）

```bash
python3 -m pip install -r requirements.txt
PYTHONPATH=src2:src3:src4 python3 src4/fuse4.py \
  --dir artifacts/v4 --submission submissions/submission_v4.csv
PYTHONPATH=src2:src3:src4 python3 src4/audit_v4.py \
  --dir artifacts/v4 --submission submissions/submission_v4.csv --target 0.707
```

---

## 6. 天花板说明

用 V3/V4 强分数交叉拟合校准风险，Bayes AUC 约 **0.7057**。  
当前诚实嵌套 **0.70303** 已贴近该估计；在不动协议红线的前提下，继续堆种子/弱世界的期望收益进入噪声带。0.707 目标门保持失败，不包装成达标。
