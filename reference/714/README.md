# 714 参考材料

来自原根目录 `714.zip`（已移除 zip，去掉 `__MACOSX` / `._*` 垃圾文件）。

```text
旧代码/          explore_d_online.py, feat_semantic.py
最新参考/        explore_best.py + 特征设计文档_best_v1.md
                 （声称双编码世界 max2，线上约 0.71464）
```

仅作只读参考；正式训练入口仍在 `src4/` / `src_beat/`（若有对应分支）。
